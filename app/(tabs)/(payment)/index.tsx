import { ThemedText } from "@/components/themed-text";
import { View } from "react-native";

const Payment = () => {
  return (
    <View>
      <ThemedText type="title">
        Esta pantalla va a mostrar todos mis métodos de pago registrados
      </ThemedText>
    </View>
  );
};

export default Payment;
