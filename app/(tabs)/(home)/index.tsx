import { ThemedText } from "@/components/ThemedText";
import { View } from "react-native";

const Home = () => {
  return (
    <View>
      <ThemedText type="title">
        Esta es la página de inicio, donde se muestran mis eventos
      </ThemedText>
    </View>
  );
};

export default Home;
